# Make Resume Point Note

## Keď sa vrátime k Make

Začni len týmto:

1. over sieť mimo Cloudflare 1010
2. spusť `teams`
3. spusť `interface`
4. spusť `logs`
5. až potom `run` s `data/qa/make_input_pack_go.json`

## Neriešiť znovu

- base URL
- token parsing v kóde
- team ID parsing v kóde
- scenario ID parsing v kóde

Tieto časti sú už pripravené a debugging je teraz pauznutý len na externom network blocker-i.
